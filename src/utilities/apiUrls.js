// public urls

const videos = "/api/videos";
const categories = "/api/categories";

// auth routs

const likes = "/api/user/likes";

const watchlater = "/api/user/watchlater";

const playlist = "/api/user/playlists";

const history = "/api/user/history";

export { videos, categories, likes, watchlater, playlist, history };
